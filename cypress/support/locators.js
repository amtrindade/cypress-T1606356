const locators = {
    LOGIN: {
        TF_ENVIRONMENT: '[name="environment"]',
        TF_USERNAME: '[name="username"]',
        TF_PASSWORD: '[name="password"]',
        BTN_LOGIN: '[name="login"]'
    },
    HOME: {
        BTN_PROFILE_WIDGET: '.profile-widget',
        LBL_TEXT_LOGIN: '.text-login'
    }
}

export default locators